import Vue from 'vue'
import { NavBar, Field, Button, CellGroup, tabbar, TabbarItem, Toast } from 'vant'

Vue
  .use(NavBar)
  .use(Field)
  .use(Button)
  .use(CellGroup)
  .use(tabbar)
  .use(TabbarItem)
  .use(Toast)
